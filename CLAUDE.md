Application must work in a serverless, static page deployment (Github Pages)

## Design Context

Design strategy lives in `PRODUCT.md` (register: product; platform: web). Read it before UI work. Core principles: the data is the interface; quiet by default; instrument, not toy; local-first is the product; no friction between intent and logging. Visual system captured in `DESIGN.md`.

